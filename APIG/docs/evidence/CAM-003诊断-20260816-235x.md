# CAM-003 诊断触发记录（非敏感）

> 时间：2026-08-16（Asia/Shanghai）  
> 入口：公网 Web `/api/agent/chat` → `yingteng-camera-support` Runtime
> 目标：验证 Agent 通过 `yingteng_test` MCP 工具集调用受 API Key 保护的存量 API。

## 请求

```json
{"message":"请诊断 CAM-003，先通过摄像头 MCP 查询设备详情、诊断指标和近期告警，再给出结论。"}
```

## 结果

- HTTP 入口返回成功响应，Runtime 标识为 `yingteng-camera-support`。
- Agent 按诊断流程尝试调用 `get_camera`、`diagnose_camera`、`list_camera_alerts`、`list_cameras`。
- 四项 MCP 调用均返回业务 API 的 `401`：`缺少或无效的 X-API-Key`。
- Agent 未编造 CAM-003 数据，明确返回“无法获取数据”的排障建议。

## 结论

本次已成功触发一次真实 Agent 诊断请求，但未通过凭据托管验收。对比 APIG 入口此前的 `无 Key=401 / 错误 Key=401 / 正确 Key=200` 结果，当前剩余问题定位在 MCP 出站凭据未被 `yingteng_test` 运行时实际注入，或 MCP 工具集仍使用旧出站配置。下一步应在控制台重新核对 `yingteng_test` → `yingteng_camera_v3` 的出站凭据绑定并重新发布/刷新 Runtime，然后重复本请求。

> 本记录不保存 API Key、Runtime Key、AK/SK 或请求头原文。
