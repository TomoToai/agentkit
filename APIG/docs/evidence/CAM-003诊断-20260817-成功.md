# CAM-003 MCP 诊断成功记录（非敏感）

> 时间：2026-08-17（Asia/Shanghai）  
> 入口：MCP 控制台调试 + 公网 `/api/agent/chat` 复测  
> 目标：确认 `yingteng_test` 出站凭据可访问受 API Key 保护的存量 API。

## 修复前后

- 修复前：MCP 工具调用返回 `401 api_key_invalid`。
- 根因：凭据托管对象虽存在，但实际值与云上存量 API 使用的 `BUSINESS_API_KEY` 未完成一致性校准。
- 修复：使用项目环境中的同名密钥更新凭据托管对象 `yingteng_business_api_key`，保留 Header `X-API-Key` 配置。

## MCP 调试结果

调用：`diagnose_camera(camera_id=CAM-003)`

- 调用状态：成功
- 网络状态：`critical`
- 丢包率：`38.4%`
- 延迟：`486ms`
- 供电：正常
- 存储：正常
- 温度：`47°C`
- 画面：间歇中断

## Agent 端到端结果

公网 `/api/agent/chat` 再次触发 CAM-003 后，Runtime 成功获取设备详情、诊断指标和 2 条活跃告警，并返回网络问题结论与排障步骤。密钥值、Runtime Key、AK/SK 和请求头原文未记录。
